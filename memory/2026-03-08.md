# 2026-03-08

- Mark 對 context 壓縮很有感，這次看到 Telegram 主 DM context 幾乎歸零；已再次確認長期 DM 很容易累積超肥上下文，之後主題做完或切換任務時要主動提醒 `/new`。
- 討論到 `memory-lancedb-pro` 的定位：它是外部記憶倉庫，不是讓當前 chat context 直接歸零的原因；真正讓 context 變小的是新 session / compaction / reset。
- 回顧 lancedb-pro 權限風險時，Mark 點出一個正確盲點：若 ownership 混搭會影響他手動維護或安裝 plugin，不能只提風險不問是否要順手修平；之後遇到這種「現在不炸但之後會炸」的權限/ownership 技術債，要主動提供修正選項。
- Mark 明講自己不具備主機管理能力，平常多靠 GPT/Gemini 教；因此擴充 OpenClaw 功能時應優先走「我代勞、少讓他碰 Linux 指令」的路線。
- 語音互動需求已明確：先做 Telegram，不先硬拚 LINE；目標是做出適合開車場景的語音 MVP（Mark 問、哈酷用語音回，內容短而可聽）。
- 已把 `/home/node/.openclaw/openclaw.json` 的 `tools.media.audio.scope` 從只 allow LINE 改成同時 allow `line` 與 `telegram`，並重啟 gateway。
- Telegram 測試結果：語音訊息已能進系統，但 transcript 變成 `Transcribe the audio.`，不是實際內容。
- 已排除「少裝 skill」的可能，確認這是 OpenClaw 內建 media/audio pipeline，不是 AgentSkill/plugin 問題。
- 找到測試音檔 `/home/node/.openclaw/media/inbound/file_5---28ad203a-562e-4588-b7d3-2aa7246bc8e7.ogg`，直接對 Groq transcription endpoint 驗證：
  - 不帶 prompt：Groq 有回辨識文字（這次回 `Thank you.`，雖然內容不準，但代表 endpoint 通）
  - 帶 `prompt=Transcribe the audio.`：Groq 直接回 `Transcribe the audio.`
- 目前最關鍵結論：Telegram 語音 pipeline 已通到 provider，但 Groq + OpenClaw 預設 audio prompt 組合會製造假 transcript；下一步應優先想辦法覆蓋/清掉這個 prompt，或改 provider。
- 進一步翻 OpenClaw dist code 確認：`tools.media.audio.prompt` 是正式支援的設定欄位，會覆蓋 audio transcription 用的 prompt。
- 但這版 OpenClaw 不能把 audio prompt 真正設成空值；空字串仍會 fallback 到預設 `Transcribe the audio.`。
- 直接對同一段 `.ogg` 音檔做 prompt A/B：英文 prompt 如 `Please transcribe verbatim.` / `Return only the spoken words.` 不會回聲 prompt；中文 prompt 反而更容易回奇怪內容。
- 已將 `/home/node/.openclaw/openclaw.json` 的 `tools.media.audio.prompt` 改成 `Please transcribe verbatim.`，並開始安全重啟 gateway，準備再做 Telegram 語音實測。
- 新一輪 Telegram 實測後，訊息裡的 transcript 從 `Transcribe the audio.` 變成 `Thank you.`；這表示 placeholder prompt 回聲問題已解，但 Groq 對中文實際辨識仍嚴重失準。
- 直接拿最新語音檔 `/home/node/.openclaw/media/inbound/file_6---497f2672-65c2-44bb-ab51-3a4d36cc91ca.ogg` 對 Groq transcription endpoint 做 A/B：
  - current prompt + no language：`Thank you.`
  - current prompt + `language=zh`：還是 `Thank you.`
  - no prompt + `language=zh`：回了完全不相干的中文內容
- 目前判斷：Telegram 音訊管線已通、OpenClaw prompt workaround 也生效，但 Groq 這個 provider 對 Mark 的中文 Telegram 語音場景不夠可靠；下一步應考慮換 STT provider（例如 Deepgram/OpenAI）而不是繼續硬磨 Groq。
- Mark 後續申請了 Speechify API，想知道能不能派上用場；已確認就目前 OpenClaw 內建支援來看，Speechify 比較偏未來的 TTS 選項，不是現在要解的 STT 問題，而且這版 docs 沒看到內建 Speechify provider。
- 已避免把使用者貼出的 Speechify API key 寫入任何記憶檔；若之後真的要用 Speechify，應重新發/旋轉金鑰後再走正式整合。
- Mark 又申請了 Deepgram API key；已直接對 Deepgram endpoint 驗證，確認 key 本身是通的。
- 研究中發現本機 Deepgram quick-start doc 與官方當前模型語系表有落差：官方資料顯示 `nova-3` 雖支援 `multi`，但中文（`zh`/`zh-TW`）實際列在 `nova-2`；因此已決定不要再用 `nova-3` 測中文 Telegram 語音。
- 對既有兩段 Telegram 語音檔（`file_5...ogg`, `file_6...ogg`）直接做 Deepgram API A/B：`nova-3`、`nova-2`、`zh-TW`、轉 WAV 後重送，都只回空 transcript。
- 最後用 `ffmpeg volumedetect` 抓到真正的大問題：這兩段 Telegram 語音檔音量幾乎是靜音（約 `mean_volume -91.0 dB`，`max_volume` 約 `-90 dB`），所以任何 STT provider 都很難正常辨識；Groq 先前回的 `Thank you.` 比較像幻覺，不是成功辨識。
- 已把 `/home/node/.openclaw/openclaw.json` 的音訊 provider 正式切到 Deepgram：
  - `models.providers.deepgram.apiKey` 已設
  - `tools.media.audio.models = [{ provider: "deepgram", model: "nova-2", language: "zh-TW" }]`
  - `tools.media.audio.providerOptions.deepgram = { punctuate: true, smart_format: true }`
  - 移除先前只為 Groq bug 加上的 audio `prompt`
- 已重啟 gateway；下一步不是再猜 provider，而是請 Mark 傳一段更大聲、更清楚、8~15 秒的新 Telegram 語音，重新驗證整條 Deepgram 路線。
- 後來 Mark 發現只要把 `openclaw.json` 內那段 `models.providers.deepgram.apiKey` 相關區塊刪掉，gateway 就不再一直重開；我回查 log 也證實這是 schema 問題，不是 Deepgram 服務本身掛掉。
- 關鍵錯誤訊息：`models.providers.deepgram.baseUrl` / `models.providers.deepgram.models` 被視為必填，因此單塞 `apiKey` 會讓 config invalid，造成 repeated reload。
- 新結論：這版 OpenClaw 接 Deepgram key 時應優先走 `DEEPGRAM_API_KEY` 環境變數，而不是把 key 塞進 `openclaw.json` 的 `models.providers.deepgram`。之後若要持久化，應依 USER.md 規則寫進 `/home/node/openclaw-host/docker-compose.override.yml`。
- Mark 接著提出一個合理逆推：既然前兩段 Telegram 語音幾乎靜音，真正問題可能是麥克風/收音，而不是 provider 本身；因此先恢復到較簡單、已知至少會跑出 transcript 的 Groq 路線，反而更適合做新一輪乾淨重測。
- 已把 `tools.media.audio` 從 Deepgram 切回 Groq：
  - `provider = groq`
  - `model = whisper-large-v3-turbo`
  - `prompt = Please transcribe verbatim.`
  - Telegram / LINE scope 保持 allow
- 已觸發 gateway safe restart，準備等下一段「更大聲、非靜音」的新 Telegram 語音再驗證。
- 新一輪 Groq 實測成功：Mark 傳來較清楚的新 Telegram 語音後，系統收到的 transcript 是 `Hello, 测试好了吗?这样可以吗?有清楚吗?`。
- 這證明前面的大坑主要真的是收音/音量，而不是 Groq 完全不能做中文語音；至少在收音正常時，Groq 這條 Telegram inbound STT 已可用。
- 目前可先把 Telegram 語音輸入 MVP 視為基本打通；接下來若要進一步優化，可再測純中文穩定度、長一點的自然口語，以及「哈酷回語音」這條 TTS 路線。
- 再補一段有背景噪音的中文語音測試後，Groq 依然成功抓到整段意思，只把「哈酷」聽成「哈古」；這表示在實際使用情境下（不是完全安靜環境）也已經有可用性。
- 原本以為 Telegram 語音輸出已通，但進一步手動發送測試後發現是誤判：`tts` 工具雖回 `[[audio_as_voice]]` + `MEDIA:/tmp/openclaw/...mp3`，實際產出的兩個 MP3 檔都只有 0 bytes。
- 因為音檔是空的，改用 `message(action=send, asVoice=true)` 手動補送時，Telegram 回 `400 Bad Request: file must be non-empty`；所以真正失敗點在 TTS backend 產檔，不在 Telegram 通道。
- 新結論：Telegram 語音互動目前是「語音輸入已通、語音輸出未通」；如果要快速補齊輸出，Speechify 反而比較適合拿來解 TTS，而不是 STT。
- Mark 要求走 B 路線，但特別提醒不要再弄壞 `openclaw.json`，而且要顧到晚上 7 點單字任務；因此改採完全隔離的 Speechify 研究方式，不動主配置檔、不動 cron/jobs。
- 已用 subagent 做隔離研究，確認 Speechify 官方最小 TTS API：
  - `POST https://api.speechify.ai/v1/audio/speech`
  - Header: `Authorization: Bearer <SPEECHIFY_API_KEY>`
  - Body 最小可用：`{"input":"Hello","voice_id":"george"}`
  - 建議顯式加 `audio_format`（如 `mp3`）
  - 非 streaming 回傳音訊在 `audio_data`（base64）
- Subagent 已在 workspace 留下不含密鑰的隔離檔案：
  - `/home/node/.openclaw/workspace/scripts/speechify_tts_probe.py`
  - `/home/node/.openclaw/workspace/notes/speechify-telegram-tts.md`
- 該腳本設計成只吃臨時環境變數 `SPEECHIFY_API_KEY`，不讀不改 `openclaw.json`；也支援把產出的音訊轉成更適合 Telegram 的 OGG/Opus。
- 本機前處理路線已驗證：`ffmpeg/ffprobe` 可用，且本機測得 `mp3 -> ogg/opus` 轉檔可產生非空、codec 正確的 Telegram 語音檔；代表 Telegram 端前處理不是主 blocker。
- 目前還沒能完成真正的 Speechify 成功出音實測，因為 subagent 執行環境沒有可用的 `SPEECHIFY_API_KEY`；但 endpoint 可達、錯誤處理正常（假 token 會回 401）。
- 目前最安全的下一步：用臨時環境變數餵 `speechify_tts_probe.py`，先驗證 Speechify 可產生非空音檔，再由主 agent 用 Telegram `asVoice` 發送，而不是先去碰 OpenClaw 主配置。
- 已完成這個實測：用一次性環境變數呼叫 `scripts/speechify_tts_probe.py`，成功產出
  - `/home/node/.openclaw/workspace/tmp/speechify-tts/speechify-probe.mp3`（非空）
  - `/home/node/.openclaw/workspace/tmp/speechify-tts/speechify-probe.telegram.ogg`（非空，codec=`opus`）
- 隨後用 `message(action=send, asVoice=true)` 成功把該 `.ogg` 當 Telegram voice note 送出，messageId=`803`；這證明 B 路線（Speechify 做語音輸出）已打通，而且全程未修改 `openclaw.json`。
- 因此目前 Telegram 語音互動狀態升級為：
  - 語音輸入：Groq STT 在收音正常時可用
  - 語音輸出：可透過隔離的 Speechify 腳本 + Telegram `asVoice` 成功送出
- 剩下的工作不再是「能不能做」，而是要不要把這條 Speechify TTS 路線包得更順手（例如做成固定腳本/工作流），同時避免把密鑰寫進主配置。
- Mark 收到 Speechify 語音後回報「是外國腔」；我回查 Speechify 官方語言支援頁，確認 Mandarin 目前列在 `Coming Soon`，所以這條雖然技術上已打通，但中文口音不自然是預期結果，不是實作失敗。
- 因此目前對 Speechify 的定位應調整成：
  - 已可當 Telegram 語音輸出的技術備援
  - 但若要追求「哈酷用自然中文回你」，還要再找更適合中文的 TTS provider（例如 OpenAI / ElevenLabs / 其他中文向方案）。 