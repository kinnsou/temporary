
## 2026-03-02 08:43 UTC — durable notes
- Enabled multi-agent routing for LINE: `main` handles Mark private LINE DM; `line-groups` handles other LINE group traffic (Codex).
- Enforced LINE quota policy in cron flows: only content message (no extra "已完成" follow-up), plus strict single-message behavior for LINE group replies.
- Telegram bot connected and paired successfully (`@HightechDogeBot`) for freer private-chat usage (non-LINE quota path).
- Sonnet hit rate limit; switched main default model to Codex temporarily, with a one-time cron set for today 15:40 (Asia/Taipei) to switch back to Sonnet and auto-delete after run.
- OpenClaw host repo update path fixed: local `docker-compose.yml` conflict handled via stash; repo fast-forwarded to latest upstream (`d0ac1b019517`); custom runtime settings kept in `docker-compose.override.yml` to avoid future merge conflicts.
- Upgrade completed successfully: runtime now on OpenClaw 2026.3.2; `openclaw update status` reports npm latest 2026.3.1 (local newer).
- Pending cleanup decision: git stash `stash@{0}` (pre-update local docker-compose.yml changes) can be dropped after confirmation.
- Applied routing simplification per user request: Telegram chats use main agent (Sonnet 4.6); all LINE traffic (DM + groups) routes to `line-groups` (Codex 5.3).
- Model defaults set back to Sonnet primary with Codex fallback for main agent; LINE stays on Codex through channel binding.
- Dropped temporary git stash (`stash@{0}`) after successful repo update and override migration.
