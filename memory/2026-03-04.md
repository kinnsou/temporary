## 2026-03-04 01:52 UTC — durable notes
- User temporarily switched to ChatGPT due to unresolved LINE inbound webhook issue (HTTP 405 on /line/webhook).
- Expected follow-up style: provide a concise final report with status/root cause/fix plan once LINE issue is fully resolved.
- Troubleshooting update: found `gateway.controlUi.enabled=false` with `basePath=/ui` in config; re-enabled Control UI and kept `/ui` base path to avoid root-path clashes while debugging LINE webhook 405.
- Live test confirmed: GET /ui works (302), but POST /line/webhook still returns 405. LINE inbound remains broken after config tweaks.
- Patched two dist bundles (gateway-cli-CUIg-zYl.js, gateway-cli-D3NQ_2yi.js): moved the 405 method check inside the /ui path block so POST /line/webhook no longer gets intercepted by Control UI handler. POST /line/webhook still returning 405 after patch — likely not the only caller of this function. Ongoing.
- Patched dist bundles (2 patches total): (1) moved controlUi 405 check inside /ui path block; (2) added /line/ prefix bypass in shouldEnforceGatewayAuthForPluginPath. Still returning 401 — gateway may have restarted from fresh image (hostname changed from 3f717dcb0cd2→b5f9afd5a070). Patches may need re-applying after container rebuild.
- Applied source-level fixes in /home/node/openclaw-host for LINE webhook auth/intercept issues: patched src/gateway/control-ui.ts and src/gateway/server/plugins-http.ts.
- Pending manual step from user: rebuild gateway container (`docker compose up -d --build openclaw-gateway`) to activate source patches, then verify LINE webhook returns non-401/405.
- User requested 3/3 vocab backfill and reminded 3/4 19:00 vocab send; also flagged repeated old word issue. Backfilled 2026-03-03 vocab in memory/daily-tasks.json and added 2026-03-03 section to daughter-vocab.md with unique words (careful, excited, practice).
- User confirmed LINE Developers Console webhook Verify succeeded after rebuild; inbound LINE webhook path is now reachable again.
- User reminded model controls: `/model openai-codex/gpt-5.3-codex` to force Codex, `/model default` to return to configured default with fallback.

## 2026-03-04 03:33 UTC — durable notes
- 已確認主設定 `/home/node/.openclaw/openclaw.json` 中 `agents.defaults.thinkingDefault=high`；`agents.defaults.verbose`、`gateway.verbose`、`agents.defaults.reasoningDefault` 目前皆未顯式設定。
- 針對 LINE 群組 session 狀態排查：`/home/node/.openclaw/agents/line-groups/sessions/` 下多個 `.jsonl` 與 `sessions.json` 曾出現 root 擁有檔案，可能影響後續會話寫入一致性。
- 已更新 cron 任務「每日英文單字（晚上7點）」提示詞（`/home/node/.openclaw/cron/jobs.json`）：
  - 嚴格限制僅 19:00~19:10 發送、當日已送則禁止重送。
  - 明確要求 3 新字 + 2 複習字，且新字避免與近 7 天重複（至少排除昨天）。
  - 僅允許發送單字正文一則，禁止完成通知/錯誤文字外發。
  - 若任何步驟失敗僅記錄並 `NO_REPLY`。
  - 明文禁止讀寫 `.learnings/*`，避免把錯誤學習檔內容外發到 LINE。
- 已停用 workspace skill `self-improving-agent`（移至 `skills-disabled/self-improving-agent`），避免代理在對話流程中自動修改/讀取學習檔造成外發風險。
- 已重置 LINE groups session mapping（刪除 `sessions/sessions.json`，並先備份），讓新會話以乾淨狀態載入更新後技能集合。
- 已對 `openclaw-gateway` 送出 `SIGUSR1` 觸發重載；本機測試 `POST http://127.0.0.1:18789/line/webhook` 回應 `200`，LINE webhook 路徑目前可正常接受 POST。

## 2026-03-04 03:44 UTC — durable notes
- 使用者詢問「修好了？」；目前確認 LINE inbound webhook 已恢復（本機 POST `/line/webhook` 回應 200）。
- 已向使用者重申後續操作：到目標 LINE 群先發一句話，以便擷取最新群組/會話 ID 並正確綁定任務。

## 2026-03-04 03:52 UTC — durable notes
- 使用者回報 LINE 群組中「單字格式偷工減料」：機器人曾回覆不合格縮寫（`today_vocab: ... / review: ...`）且缺少發音/例句。
- 追查到 line-groups 該群 session（`70b00bc5-...`）在互動修正時引用到 `daily-tasks.json` 中 2026-03-02 的簡化樣式，導致錯誤示範被延續。
- 已修正 `/home/node/.openclaw/openclaw.json`：新增群組 `C9f96037238840fdea144e4be8676d63e` 的 `systemPrompt`，明確禁止 `today_vocab/review` 縮寫，要求單字卡完整格式（中文同音+重音+意思+例句）。
- 已修正 `/home/node/.openclaw/workspace/memory/daily-tasks.json` 中 `2026-03-02.vocab.words` 為完整單字卡格式，避免未來參考到簡化模板。
- 已重置該群會話映射（刪除 `sessions.json` 對應鍵並封存舊 jsonl 為 `.bak`），避免舊上下文延續。
- 已送 `SIGUSR1` 讓 gateway 重載設定。

## 2026-03-04 03:57 UTC — durable notes
- 使用者詢問是否完工；已回覆目前為可驗收狀態，並請使用者在家庭群再發一句進行最終即時驗收。
- 相關修正已提交到 workspace git：`624df97`（harden LINE vocab format and log fixes），包含 `memory/daily-tasks.json` 與 `memory/2026-03-04.md` 更新。

## 2026-03-04 04:04 UTC — durable notes
- 針對家庭群單字格式異常完成根因判斷：主要非 thinking off，而是 LINE 群組 `systemPrompt` 可能因 groupId 大小寫/鍵名匹配未命中，導致回覆偏離固定格式。
- 已在 `/home/node/.openclaw/openclaw.json` 對同一群組補齊 4 種鍵名（大小寫與 `group:` 前綴）並寫入同一套「3+2 完整單字卡」硬規範。
- 已重置該群最新 session 映射並封存舊 session jsonl，避免延續錯誤上下文。
- 已重載 gateway（SIGUSR1）使新規則生效，並請使用者再做一次群組即時驗收。

## 2026-03-04 04:12 UTC — rescue postmortem keys from user recap
- 昨日救援核心結論：不是整套 OpenClaw 壞掉，而是「舊 gateway 仍健康運作 + 新 compose 重建流程被 CLI 幽靈容器卡住」造成觀感混亂。
- 重要辨識訊號：
  - `openclaw-openclaw-gateway-1` 持續 healthy，`/healthz` 回 200。
  - `GET /` 回 404 在該版本屬正常現象（非 UI 首頁）；不可用此判斷服務掛掉。
  - Canvas 掛載正常但需 bearer token；401 多為未授權而非服務故障。
- LINE 事故根因（2026.3.2 期間）：
  - Control UI 路徑攔截造成 webhook POST 405。
  - Plugin path auth 判斷把 `/line/webhook` 誤判為需 gateway token，導致 401。
- 修復策略與教訓：
  - 先確認「既有活服務是否健在」再動重建，避免誤判為全毀。
  - 對 webhook 問題分層檢查（路由匹配 vs auth 驗證），避免一次改太多層。
  - 在「保系統優先」模式下，不做破壞性升級；先保留既有 skills/tasks/memory 生態。
- 目前狀態（依使用者回顧）：Telegram、LINE 主動推播、排程、memory-lancedb-pro 均已恢復可用；LINE inbound webhook 已修復。

## 2026-03-04 04:13 UTC — durable notes
- 使用者提供完整「2026-03-04 救援紀錄」並主動要求檢視是否需要萃取關鍵問題；偏好把事故復盤沉澱成可重用知識而非只留對話。
- 已回覆並確認採納 5 個關鍵復盤點（健康檢查優先、幽靈容器根因、LINE 401/405 分層、Canvas 401 判讀、先保運作後升級）。
- 後續可交付項目：整理一頁式故障排查 SOP（先看什麼／不要先做什麼）供下次快速定位。

## 2026-03-04 04:22 UTC — durable notes
- 使用者詢問是否可分模型設定 thinking（Sonnet 設 medium、Codex 設 high）以控管 Sonnet 成本；已確認可行。
- 關鍵設定知識已回覆：`agents.defaults.models["<provider/model>"].params.thinking` 可做模型級覆寫，優先於全域 `agents.defaults.thinkingDefault`；但仍會被 session 的 `/think` 覆寫。
- 已向使用者說明 170k 字元換算 token 的粗估範圍（英文約 42.5k token；中文可能顯著更高），並指出大上下文會快速耗盡 Sonnet 額度。
