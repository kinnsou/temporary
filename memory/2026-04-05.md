# 2026-04-05

- OpenClaw config updated: `main` agent `thinkingDefault` set to `xhigh`; `line-groups` agent `thinkingDefault` set to `medium`.
- Reason: Mark wants Codex to fully carry Telegram main chat and LINE group work after Claude support was removed from active use.
- `cron-tasks` left unchanged.
