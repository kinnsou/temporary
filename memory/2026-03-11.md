# 2026-03-11

- 新 session 啟動。
- 確認目前模型已可切到 openai-codex/gpt-5.4，但在 OpenClaw 2026.3.2 仍無法設為 think xhigh（需額外 patch）。
- 已著手恢復 Telegram 語音流程：保留 Groq STT（telegram scope + prompt workaround），並把 BTC 語音播報 cron 改回 3.2 可用模型（gpt-5.3-codex，thinking=low）。
- BTC 語音 cron（f3111f17-8936-4ca7-ad87-4f8cd7f7f15b）已重新啟用，09:45 這輪執行狀態為 `lastRunStatus=ok`。
- 目前 Speechify API key 不在 `~/.openclaw/.env`（`SPEECHIFY_API_KEY_MISSING`），語音播報會依 job 規則退回文字訊息。
- 嘗試直接 patch `/usr/lib/node_modules/openclaw/dist/thinking-44rmAw5o.js` 失敗：`PermissionError: [Errno 13]`，代表當前權限無法改系統安裝檔；需 root/sudo 路徑才能補上 gpt-5.4 xhigh。
- 已確認 Speechify key 實際在 `/home/kurohime/openclaw/.env`（不是 `~/.openclaw/.env`）。
- 已更新 `scripts/speechify_tts_probe.py`：若 process env 無 key，會 fallback 讀取 `~/.openclaw/.env` 與 `/home/kurohime/openclaw/.env`；本機實測可成功產出非空 MP3/Telegram OGG。
- 針對 `memory-lancedb-pro` 將 embedding endpoint 從 `http://host.docker.internal:11434/v1` 改為 `http://127.0.0.1:11434/v1`，重啟後 log 顯示 `embedding: OK, retrieval: OK`。
- 曾出現 `openclaw gateway status` 的 RPC probe `1006 abnormal closure`；2026-03-11 11:39 以 `openclaw gateway install --force`（重寫 systemd service）修復，UI 與 `RPC probe: ok` 恢復正常。
- 新教訓：遇到 OpenClaw 升級後的版本/env/路徑/profile 變更，不能只 `openclaw gateway restart`；需要 `openclaw gateway install --force` 先重寫 systemd service，再驗證 status。
- BTC cron 已改為用 CLI 正式建立：`606e703d-d331-424a-8a9f-e722b6f01d0d`（every 15m, isolated + announce 到 Telegram 8523715171），手動 run 後 `delivered=true`。
- 舊的失效 job `f3111f17-8936-4ca7-ad87-4f8cd7f7f15b` 已移除，避免誤用。
- 新教訓：不要手改 `~/.openclaw/cron/jobs.json`（gateway 可能用記憶態覆蓋回去）；要用 `openclaw cron add/edit/enable/disable/rm`。
- BTC 任務 `606e703d-d331-424a-8a9f-e722b6f01d0d` 已校正為準點 cron：`*/15 * * * *`（Asia/Taipei，`staggerMs=0`），下一次觸發固定在整點 00/15/30/45。
