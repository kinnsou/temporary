# 2026-04-10

- Mark 指示：目前不要再走 ACPX 與 Claude Code 協作，短期不修；若要對 Claude Code 討論或協作，改用本機 CLI 直接跑。
- 已依 README 的 user-level 方式安裝 Claudeception 到 `~/.claude/skills/claudeception`，並把 activation hook 掛到 `~/.claude/settings.json` 的 `hooks.UserPromptSubmit`。
