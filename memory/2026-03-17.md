## 2026-03-17
- Mark 明確要求：`C:/TASK` 是線上交易執行中的程式目錄（內含 live trader），之後不要直接在這個目錄用目前 Ubuntu / WSL 的系統 Python 去執行 runner。
- 正確做法：在 Ubuntu 內維持一份獨立的 Python 執行環境，並把需要測試/回測的 runner 複製到隔離的鏡像目錄後再跑，與線上交易中的 `C:/TASK` 分開。
- 這次查 4 幣最後一筆報表時，我誤用目前 WSL 的系統 Python 直接碰 `C:/TASK`，因此撞到 `numpy` 缺失；之後這類任務要先切回隔離環境。 
