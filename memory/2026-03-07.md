# 2026-03-07 記憶日誌

## 2026-03-07 00:59 UTC — durable notes（延續昨日工作）
- 使用者確認 lesson12-202603w2.html 試作完成品品質良好，指定直接用於下週三發送，不再重做。
- 已更新周三帶讀故事 cron（id: 72d8819a）prompt，新增「先判斷檔案是否已存在」邏輯：
  - 若檔案存在（w2 已有）→ 直接發連結，禁止重新生成
  - 若不存在 → 才生成新頁面再 git push 再發連結
- 下周三（2026-03-11）預計行為：偵測到 `lesson12-202603w2.html` 存在 → 直接發送 https://kinnsou.github.io/temporary/lesson12-202603w2.html 到群組 C47614277d11b25b5f21e6974ef29aed2。

## 2026-03-07 01:20 UTC — durable notes
- 使用者提供 PR #36590 分析文件，請求在本機 OpenClaw 加入 GPT-5.4 支援。
- 已完成 Minimal Runtime Patch（build 通過）：
  - `src/agents/model-forward-compat.ts`：加入 `openai/gpt-5.4`、`openai/gpt-5.4-pro`、`openai-codex/gpt-5.4` resolver（含 context 1_050_000、maxTokens 128_000、reasoning=true）
  - `src/auto-reply/thinking.ts`：`XHIGH_MODEL_REFS` 加入三種 gpt-5.4 ref
  - `src/agents/live-model-filter.ts`：`OPENAI_MODELS` 加入 `gpt-5.4`、`gpt-5.4-pro`
  - `src/commands/openai-codex-model-default.ts`：預設模型從 `gpt-5.3-codex` 改為 `gpt-5.4`
- 未做（Full Parity）：`extra-params.ts` serviceTier、`model-catalog.ts`、`models list` 相關、`model-auth.ts`
- 待辦：使用者若要啟用，需在 `openclaw.json` 將 model 改為 `openai-codex/gpt-5.4` 並重啟 gateway。

## 2026-03-07 01:36 UTC — durable notes
- 使用者在 Telegram 嘗試 `/model openai-codex/gpt-5.4`，收到錯誤："Model is not allowed"。
- 根本原因：build 成功但 gateway 仍跑舊版 bundle（hash-based Vite 打包，需重啟才生效）。
- 已執行 `gateway-safe-restart.sh`（SIGUSR1 方式）→ 重啟成功（01:25 UTC），LKG 已更新。
- 下一步：驗證重啟後 `/model openai-codex/gpt-5.4` 是否可切換成功。

## 2026-03-07 01:55 UTC — durable notes
- 已再次驗證：gpt-5.4 patch 確實存在於 gateway 使用的 dist bundle（`model-CVldAt2z.js` 含 `resolveOpenAIGpt54ForwardCompatModel` 與 resolver chain）。
- 已請使用者重新在 Telegram 測試 `/model openai-codex/gpt-5.4`。
- 若仍報 not allowed，下一步將檢查 auth profile / model allowlist / models list 路徑是否仍有 parity 缺口。

## 2026-03-07 02:20 UTC — durable notes
- 針對 `/model openai-codex/gpt-5.4` 仍顯示 not allowed，已定位真正阻塞點：
  - `openclaw.json` 的 `agents.defaults.models` 是非空 allowlist，且只允許 `openai-codex/gpt-5.3-codex` + `anthropic/claude-sonnet-4-6`。
  - 因此即使 gpt-5.4 runtime patch 存在，/model 仍會被 allowlist 拒絕。
- 已修正 `/home/node/.openclaw/openclaw.json`：
  - `agents.defaults.model.primary` 改為 `openai-codex/gpt-5.4`
  - `fallbacks` 保留：`anthropic/claude-sonnet-4-6`, `openai-codex/gpt-5.3-codex`
  - `agents.defaults.models` 新增 `openai-codex/gpt-5.4`（thinking=high）
- 已完成 JSON 語法驗證（JSON_OK）並執行 gateway-safe-restart（01:58 UTC 成功）。
- 待使用者再測 Telegram `/model openai-codex/gpt-5.4` 實際切換結果。

## 2026-03-07 02:40 UTC — durable notes
- 使用者回報 `/model openai-codex/gpt-5.4` 已成功，確認 GPT-5.4 切換生效。
- 結論：本機 OpenClaw 已完成 gpt-5.4 啟用（runtime patch + allowlist 放行 + default primary 改為 gpt-5.4 + gateway 重啟）。
- 後續可選驗證：測試 `thinking=xhigh` 在 `openai-codex/gpt-5.4` 是否也能正常運作。

## 2026-03-07 02:43 UTC — durable notes
- 使用者回指 earlier lesson12 失誤案例。
- 已再次對齊結論：當次問題不是單純 Sonnet/Codex 完全失聯，而是我手動代做周三任務時，未先查 cron prompt 規格，導致漏掉難度與 4 區塊要求。
- 目前保護措施：
  1. `AGENTS.md` 已加入「手動執行排程任務前必查 cron payload.message」強制規則
  2. 周三 cron 已改成先檢查本週 HTML 是否已存在；存在就直接發連結，不重做
- 可選後續：若使用者要更穩，可把 lesson12 成品再抽成周三頁面樣板參考。

## 2026-03-07 02:59 UTC — durable notes
- 使用者詢問英文單字任務是否也有「不同腦不同資料庫」問題，因為常看到 3 個新字中有舊字重複。
- 已查核：英文單字任務目前共用同一份 workspace 資料，不是 Sonnet/Codex 各自分開資料庫。
  - 來源檔案：`daughter-vocab.md` + `memory/daily-tasks.json`
- 已定位較可能原因：
  1. cron 規格只要求避開「前 7 天」新字，不是全歷史唯一
  2. 去重依賴文字理解（markdown/json 文字內容），不是結構化 registry 做硬比對
- 已提出改進方向：新增結構化 `memory/vocab-history.json` 作為單字總表，讓「新字永不重複、複習字才可重出」成為硬規則。

## 2026-03-07 03:04 UTC — durable notes
- 使用者重新定義英文單字任務規格：
  1. 新字範圍限「台灣普遍認定國中必會1000單」
  2. 每日 3 個新字永不重複，直到 1000 單全部學完
  3. 2 個複習字改為「引導式填空 + 答案 + 文法提示」，不再只是問是否記得單字
  4. 複習要趁機教搭配與句子拆解（如片語、S+V、S+V+O 等）
- 已落地兩項變更：
  - 建立權威字庫：`memory/vocab-history.json`（目前已從既有記錄初始化 29 個字）
  - 更新 cron `每日英文單字（晚上7點）` prompt：要求使用 `vocab-history.json` 做硬去重，並用 guided cloze + grammar hint 格式出複習題
- 結論：LINE 單字任務腦已同步知道新規則，不再只靠 markdown 文字理解去重。

## 2026-03-07 04:24 UTC — durable notes
- 依使用者提供的既有單字卡格式（picnic / clever / promise + excellent / discover），已示範新版「複習挑戰」輸出：
  - 複習題改成中文提示 + 英文填空 + 解答 + 文法提示
  - 範例：
    - excellent → `Her English is ____________.`（S+V+C，形容詞補語）
    - discover → `We ____________ a small path in the forest.`（S+V+O，動詞 + 受詞）
- 已提議進一步優化成更有教學感的 LINE 發送版：
  1. 複習段改名為闖關式標題（如「🧠 複習挑戰」）
  2. 文法提示寫得更像老師帶讀，而不只抽象標註 S+V+C / S+V+O

## 2026-03-07 04:37 UTC — durable notes
- 使用者親自重排了新版 LINE 單字卡格式，方向明確：
  - 視覺簡潔、看起來不多，但實際收穫多
  - 英文文法重點放在「動詞定位」
  - 暫時不要出現 S+V / S+V+O / S+V+C 這類術語給小朋友
- 使用者指定的新格式：
  - `📚 今日單字`
  - 每個新字含：單字、`📢 發音`、`📖 意思`、例句、中文翻譯
  - `🧠 複習挑戰`
  - 每題含：英文填空、中文句子、`🔍 句型觀察`
- 句型觀察要求：用白話指出 be 動詞後形容詞、動詞過去式、受詞等概念，不用術語轟炸。
- 已示範一版，使用者方向上認可；下一步可把 cron prompt 格式正式改成這個精簡教學版。

## 2026-03-07 04:45 UTC — durable notes
- 使用者進一步細修 LINE 單字卡格式：
  - 新字區只保留 emoji，不要出現「發音」「意思」文字標籤
  - 第一個 emoji 改為麥克風/音量類（🎙️/🎤/🔊）
  - 第二個 emoji 改為標籤類（🏷️）
- 複習區規則再收斂：
  - 題目下方不可直接放英文答案，避免放棄思考
  - `🔍 句型觀察` 只能用中文詞義提示，不可直接寫出被考英文單字
  - 範例：`（發現）這裡要用過去式，發現了。後面接受詞 a small path。`
- 已把上述格式同步寫回 LINE 單字 cron prompt，確認 `jobs.json` 仍為合法 JSON。

## 2026-03-07 05:18 UTC — durable notes
- 已正式建立並打包兩個可重用 skill：
  1. `line-vocab-maximizer`
     - 固化女兒 LINE 單字任務思路：1000 單範圍、3 新字永不重複、2 複習改 guided cloze、動詞定位優先、輕量 emoji 格式
  2. `openclaw-model-forward-compat`
     - 固化未來新模型導入/排錯流程：runtime patch、xhigh、build、gateway restart、allowlist/config 檢查、驗證步驟
- 兩個 skill 均已 package 成 `.skill`：
  - `skills/dist/line-vocab-maximizer.skill`
  - `skills/dist/openclaw-model-forward-compat.skill`
- 也已把 GPT-5.4 修復中的 reusable 關鍵 lesson 寫進第二個 skill：
  - 不能只 patch resolver
  - `/model ... is not allowed` 常是 `openclaw.json` allowlist 阻塞
  - 必須沿著錯誤字串追到 model-selection / config 層
